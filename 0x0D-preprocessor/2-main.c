#include <stdio.h>
/**
 * main - function that prints the file name it was complied from
 * __FILE__: preprocessor directive
 *
 * Return: Always 0 (success)
 */

int main(void)
{
	printf("%s\n", __FILE__);

		return (0);
}
